name: building-blocks

on: push

jobs: 
    echo-hello:
        runs-on: ubuntu-latest
        steps: 
            - name: say hello
              run: echo "Hello, World"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Step
              run: |
                echo " i will fail"
                exit 1
            - name: say good bye
              run: echo "Good bye"

